apiVersion: v1
kind: Service
metadata:
  name: netflix-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing

spec:
  selector:
    app: netflix-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: LoadBalancer
